# 🤖 多智能体代码生成系统

一个基于多智能体协作的自动化代码生成系统，能够从自然语言描述出发，自动生成完整的软件项目代码仓库。

## 🏗️ 系统架构

```
┌──────────────────────────────────────────────────────────────────┐
│                         用户需求输入                              │
└────────────────────────────────┬─────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────┐
│                      协调器 (Coordinator)                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │ 消息总线    │  │ 任务管理器  │  │ 状态管理器              │  │
│  │ MessageBus  │  │ TaskManager │  │ StateManager            │  │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘  │
└────────────────────────────────┬─────────────────────────────────┘
                                 │
         ┌───────────────────────┼───────────────────────┐
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  规划智能体     │    │  代码智能体     │    │  审查智能体     │
│  Planner Agent  │    │  Coder Agent    │    │ Reviewer Agent  │
├─────────────────┤    ├─────────────────┤    ├─────────────────┤
│ • 需求分析      │    │ • 代码生成      │    │ • 静态分析      │
│ • 架构设计      │    │ • 模板引擎      │    │ • 代码审查      │
│ • 任务拆解      │    │ • 依赖处理      │    │ • 测试执行      │
│ • 技术选型      │    │ • 文件输出      │    │ • 质量评分      │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────┐
│                       生成的项目代码仓库                          │
└──────────────────────────────────────────────────────────────────┘
```

## ✨ 核心特性

### 🎯 项目规划智能体 (Planner Agent)
- 分析自然语言需求描述
- 设计软件架构和技术栈
- 拆解需求为具体开发任务
- 确定任务依赖关系和优先级

### 💻 代码生成智能体 (Coder Agent)
- 根据任务指令生成完整代码
- 内置常用项目模板库
- 支持多种编程语言
- 生成配置文件和文档

### 🔍 代码评估智能体 (Reviewer Agent)
- 静态代码分析 (Python, JavaScript, HTML, CSS)
- 代码质量评分 (0-10分)
- 安全性检查
- 自动化测试执行

### 🔄 协调器模块 (Coordinator)
- 消息总线：智能体间通信
- 任务管理：调度和状态跟踪
- 状态管理：项目生命周期管理
- 进度可视化

## 📦 安装

```bash
# 克隆项目
cd codingagent

# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/macOS
# 或 venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt
```

## 🚀 使用方法

### 方式一：运行演示

```bash
python run_demo.py
```

这将自动生成一个 arXiv 论文浏览器项目到 `generated_projects/arxiv_browser` 目录。

### 方式二：命令行使用

```bash
# 使用默认需求（arXiv 浏览器）
python main.py

# 指定需求描述
python main.py -r "创建一个待办事项管理应用"

# 从文件读取需求
python main.py -r requirements.txt

# 指定输出目录
python main.py --demo -o ./my_project

# 交互式模式
python main.py -i
```

### 方式三：作为库使用

```python
import asyncio
from coordinator import Coordinator
from agents import PlannerAgent, CoderAgent, ReviewerAgent

async def generate_project():
    # 初始化协调器
    coordinator = Coordinator()
    
    # 注册智能体
    coordinator.register_agent("planner", PlannerAgent())
    coordinator.register_agent("coder", CoderAgent())
    coordinator.register_agent("reviewer", ReviewerAgent())
    
    # 定义需求
    requirement = """
    创建一个博客系统，需要：
    1. 文章列表和详情页
    2. 分类标签功能
    3. 评论系统
    """
    
    # 运行生成
    state = await coordinator.run(requirement, "./output")
    
    print(f"生成了 {len(state.files)} 个文件")

asyncio.run(generate_project())
```

## 📁 项目结构

```
codingagent/
├── README.md              # 项目说明
├── requirements.txt       # Python 依赖
├── config.py             # 配置管理
├── models.py             # 数据模型定义
├── coordinator.py        # 协调器模块
├── main.py               # 主入口
├── run_demo.py           # 演示脚本
├── agents/               # 智能体模块
│   ├── __init__.py
│   ├── base.py           # 基础智能体类
│   ├── planner.py        # 规划智能体
│   ├── coder.py          # 代码智能体
│   └── reviewer.py       # 审查智能体
└── generated_projects/   # 生成的项目输出目录
    └── arxiv_browser/    # 示例: arXiv 论文浏览器
```

## 🔧 配置

### 环境变量

创建 `.env` 文件配置 API 密钥：

```env
# OpenAI (可选)
OPENAI_API_KEY=your_openai_key

# Anthropic (可选)
ANTHROPIC_API_KEY=your_anthropic_key
```

### 配置选项

在 `config.py` 中可以自定义：

```python
from config import Config, LLMConfig

config = Config()

# LLM 配置
config.agent.planner_llm = LLMConfig(
    provider="openai",
    model="gpt-4",
    temperature=0.7
)

# 系统配置
config.system.output_dir = "./my_output"
config.system.max_retries = 3
config.system.enable_testing = True
```

## 📋 示例输出

运行演示后，将生成完整的 arXiv 论文浏览器项目：

```
generated_projects/arxiv_browser/
├── app.py              # Flask 主应用
├── arxiv_client.py     # arXiv API 客户端
├── requirements.txt    # 依赖列表
├── README.md           # 项目文档
├── .gitignore
├── templates/          # HTML 模板
│   ├── base.html
│   ├── index.html
│   ├── category.html
│   ├── paper.html
│   ├── search.html
│   ├── 404.html
│   └── 500.html
└── static/
    ├── css/style.css   # 样式表
    └── js/main.js      # 前端脚本
```

### 启动生成的项目

```bash
cd generated_projects/arxiv_browser
pip install -r requirements.txt
python app.py
# 访问 http://localhost:5000
```

## 🔌 扩展开发

### 添加新智能体

```python
from agents.base import BaseAgent

class MyAgent(BaseAgent):
    def __init__(self):
        super().__init__("my_agent")
    
    async def process(self, input_data):
        # 自定义处理逻辑
        result = await self.call_llm("你的提示词")
        return result

# 注册智能体
coordinator.register_agent("my_agent", MyAgent())
```

### 添加代码模板

在 `agents/coder.py` 的 `CodeTemplates` 类中添加模板方法。

## 🧪 测试

```bash
# 运行测试
pytest tests/ -v

# 运行代码检查
flake8 .
black --check .
```

## 📄 许可证

MIT License

## 🙏 致谢

- OpenAI / Anthropic - LLM API 支持
- Flask - Web 框架
- Rich - 终端美化库

---

**⚠️ 注意**: 本系统生成的代码仅供参考和学习使用，请在生产环境使用前进行充分测试。
